
runs:
  using: "composite"
  steps:

    - name: Start install
    run: echo Building of node application is starting
    shell: bash
    - name: install dependencies
    run: npm install
    shell: bash
      